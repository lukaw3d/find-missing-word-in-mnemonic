{
  "scripts": {
    "start": "parcel serve --no-hmr index.html",
    "build": "rm -rf docs && parcel build --no-optimize --no-scope-hoist --no-source-maps --public-url /find-missing-word-in-mnemonic --dist-dir docs index.html"
  },
  "dependencies": {
    "bip39": "^3.0.4"
  },
  "devDependencies": {
    "buffer": "^5.5.0||^6.0.0",
    "parcel": "^2.0.1"
  }
}
